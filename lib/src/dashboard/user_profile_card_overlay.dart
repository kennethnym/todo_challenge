import 'package:flutter/material.dart';

/// A widget that overlays [UserProfileCard] on top of everything else.
class UserProfileCardOverlay extends StatelessWidget {
  const UserProfileCardOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
